#include "main.h"

/**
 * add - Adds two values
 * @a: the first number to be computed
 * @b: the second number to be computed
 * Return: the integer result
 */

int add(int a, int b)
{
	return (a + b);
}
