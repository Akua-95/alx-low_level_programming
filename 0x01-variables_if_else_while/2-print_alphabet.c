#include <stdio.h>

/**
 * main	-	prints the alphabet in lowercase
 * Return:	Always (Success)
 */
int	main(void)
{
	char	lc;

	for	(lc	=	 'a';	 c <=	'z';	lc++)
	{
		putchar(lc);
	}
		putchar('\n');
		return	(0);
}



